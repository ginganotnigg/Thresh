import { TestAttemptsQueryRepo } from "../test-attemps.query-repo";

describe("test-attempts.query-repo", () => {
	beforeAll(() => {
		// Initialize the database connection or any other setup needed
	});

	it(TestAttemptsQueryRepo.prototype.getParticipantsAggregate.name, () => {

	});
});